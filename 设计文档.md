# Web-GUI-Agent 设计文档

## 1. 目标与总体方案

### 1.1 范围

系统接收自然语言任务及可选起始地址，在隔离的浏览器上下文中执行网页查询、信息提取、表单填写和跨页操作；每一次操作均需验证并记录，最终返回结构化结果与可回放的执行记录。

首期只覆盖受控测试站点和已授权站点，不自动处理验证码、MFA、支付、删除或其他不可逆操作。此类操作应返回 `NEEDS_CONFIRMATION`，等待调用方确认或人工接管。

### 1.2 技术选型

| 层次 | 选型 | 原因 |
| --- | --- | --- |
| 运行时与语言 | Python 3.12+（asyncio + 严格类型检查） | 本地运行，适合后续模型评测、数据处理和 AI 工具链；通过 `asyncio` 管理浏览器并发。 |
| HTTP 服务 | FastAPI + Uvicorn | 原生异步、OpenAPI 文档和 Pydantic 集成，适合任务 API 与健康检查。 |
| 浏览器自动化 | Playwright Python（本机 Chromium） | 支持 Browser Context 隔离、Page/Popup 生命周期、trace 和稳定 Locator；首期不依赖远程 CDP 或浏览器池。 |
| Page-Agent 参考层 | 独立 `PageAgentBridge` 适配器 | 借鉴其页面语义理解和 Agent 循环思想，但不让领域层依赖其前端运行时或私有 API。 |
| LLM 适配 | 自定义 `ModelClient` 接口 | 业务逻辑不绑定特定模型/厂商，测试可替换为 fake client。 |
| 数据校验 | Pydantic v2 | 校验 API 输入、模型输出和工具参数，避免不可信数据直达浏览器。 |
| 持久化 | SQLite（本地） | 执行记录与任务状态可查询；通过仓储接口保留未来替换数据库的可能。 |
| 快照工件 | 本地文件系统 | 存放 DOM、截图、trace；元数据只保存相对路径与校验和。 |
| 日志与指标 | structlog + OpenTelemetry | 每个 task/run/step 可关联，便于定位超时与失败。 |
| 测试 | pytest + pytest-playwright | 前者验证纯逻辑与接口，后者通过受控页面验证真实浏览器行为。 |

### 1.3 分层与调用路径

```text
HTTP API
  -> TaskService（创建、查询、取消）
  -> RunScheduler（并发、超时、取消、资源回收）
  -> AgentRunner（状态机）
       -> Observer -> Planner(ModelClient) -> ToolExecutor -> ActionVerifier
       -> TabManager / BrowserWorker
       -> RunRepository / ArtifactStore / EventLogger
```

依赖只能向内层流动：`api` 依赖 `application`，`application` 依赖 `domain` 与 ports（`Protocol` 接口），`infrastructure` 实现 ports。领域模型不得直接导入 FastAPI、Playwright、数据库或模型 SDK。

### 1.4 本地部署边界

- 服务以单个 Uvicorn 进程运行，默认只监听 `127.0.0.1`；不对局域网或公网暴露。启动命令统一由 `Makefile` 或 `scripts/run.ps1` 封装，底层为 `uv run uvicorn web_gui_agent.api.server:app --host 127.0.0.1 --port 8000`。
- 使用本机由 `playwright install chromium` 安装的 Chromium。每个任务新建一个 Browser Context；同一进程复用一个 Browser 实例。默认并发数为 2，并通过 `MAX_CONCURRENT_RUNS` 配置，避免本机内存被耗尽。
- 可写运行数据集中在工作区的 `var/`：`var/web-gui-agent.db`（SQLite）、`var/artifacts/<run_id>/`（DOM、截图、trace）和 `var/logs/`（结构化日志）。`var/` 必须加入 `.gitignore`，不得写入源码目录。
- 配置放在 `.env`，仓库只提交 `.env.example`。模型密钥、Cookie、登录口令不得进入 SQLite、日志、快照、Git 或 API 响应。
- 服务停止时应拒绝新任务、向运行中任务发出取消事件、等待有限宽限期后关闭所有 Browser Context 和 Browser；本地进程异常退出时，启动清理逻辑仅清理已完成运行的过期工件，不删除未完成任务证据。

### 1.5 Page-Agent 二开策略

本项目以 Page-Agent 为页面 Agent 能力的参考实现，但目标是**后端执行服务**，不是将其前端页面组件或浏览器端状态直接迁入 FastAPI。二开后应保持以下边界：

```text
后端 AgentRunner
  -> PageAgentBridge（可选语义页面能力）
  -> Playwright BrowserWorker（唯一的浏览器操作入口）
  -> 本机 Chromium / Page
```

- 借鉴的能力：将真实页面压缩为模型可消费的语义 observation、依据页面反馈进行 Observe → Plan → Act 循环、以及以元素语义而非坐标为中心的交互。
- 后端新增的能力：任务排队与并发控制、独立 Browser Context、跨 Tab 记忆、超时与取消、动作验证、持久化审计、失败回放、权限控制。这些由本项目负责，不能寄希望于页面侧运行时。
- `PageAgentBridge` 是可选实现。若验证后 Page-Agent 可作为页面内注入脚本或库使用，适配器仅负责获取语义 observation 或辅助生成定位提示；所有导航、点击、填写和提交仍必须经 `BrowserWorker` 和 `ToolRegistry` 白名单。
- `Observation`、`Action`、`VerificationResult` 是本项目的稳定协议。Page-Agent 返回的数据必须转换为这些领域模型，不得在 `application` 或 `domain` 传递 Page-Agent 专有对象。
- 首期必须保留纯 Playwright 的 `ObservationBuilder` 作为无 Page-Agent 回退路径。这样可以独立测试、避免上游版本变动阻塞主流程，也便于比较 Page-Agent 增强前后的成功率与 token 成本。
- 未完成许可证、版本、安装方式、公开 API、注入安全性和本机 Chromium 兼容性核验前，不复制或修改其源码；先通过独立 adapter 的契约测试确认可复用边界。

## 2. 模块、目录与职责

### 2.1 推荐目录

```text
src/web_gui_agent/
  api/
    server.py                 # FastAPI 装配与启动
    routes/tasks.py           # 创建、查询、取消任务的 HTTP 路由
    schemas/task.py           # 请求/响应 Pydantic 模型
  application/
    task_service.py           # 任务生命周期用例
    run_scheduler.py          # 并发槽位、排队、取消与总超时
    agent_runner.py           # 单次执行状态机编排
    retry_policy.py           # 依据错误类别决定是否及如何重试
  domain/
    task.py                   # Task、TaskInput、TaskStatus
    run.py                    # Run、RunStatus、StepRecord
    observation.py            # Observation、ElementCandidate
    action.py                 # Action、ToolResult、VerificationResult
    errors.py                 # 领域错误类型和错误码
    ports/
      model_client.py         # LLM 规划接口（Protocol）
      browser_session.py      # 浏览器会话抽象（Protocol）
      task_repository.py      # 任务/执行记录仓储抽象（Protocol）
      artifact_store.py       # 快照工件存储抽象（Protocol）
      clock.py                # 便于测试的时间接口
  browser/
    playwright_session.py     # BrowserSession 的 Playwright 异步实现
    browser_worker.py         # 单 Page 的安全浏览器原子操作
    tab_manager.py            # Page/Popup 生命周期与当前页管理
    observation_builder.py    # DOM、可见性、布局压缩为 Observation
    locator_resolver.py       # 从 ElementRef 解析稳定 Locator
    page_waiter.py            # 导航、网络空闲、元素稳定等待
    page_agent_bridge.py      # 将可选 Page-Agent 页面语义转换为 Observation
  tools/
    tool_registry.py          # 白名单工具注册与参数校验
    navigate.py               # 导航
    click.py                  # 点击
    fill.py                   # 填写
    select.py                 # 选择器操作
    extract.py                # 文本/属性提取
    scroll.py                 # 滚动
  verification/
    action_verifier.py        # 校验编排
    url_verifier.py           # URL 规则
    dom_verifier.py           # DOM/元素变化规则
    form_verifier.py          # 输入值规则
    feedback_verifier.py      # 成功、报错、弹窗规则
  infrastructure/
    page_agent/
      injected_bridge.py      # Page-Agent 注入/调用实现；只依赖 browser 与 ports
      mapper.py               # Page-Agent 专有结果到领域模型的映射
    model/openai_client.py    # 某一模型供应商实现（可替换）
    persistence/sqlite_repository.py
    persistence/postgres_repository.py
    storage/local_artifact_store.py
    storage/s3_artifact_store.py
    telemetry/structured_logger.py
  config/
    settings.py               # pydantic-settings 环境变量解析与默认值
  composition_root.py         # 唯一允许选择具体基础设施实现的位置
  main.py                     # 进程入口，只调用 composition root
pyproject.toml                # 依赖、工具及测试配置
.env.example                  # 本地环境变量模板（不含机密）
scripts/
  run.ps1                     # 本地启动 FastAPI 服务
  test.ps1                    # 本地执行静态检查与测试
var/                          # 本地运行数据；加入 .gitignore，不提交
tests/
  unit/                       # 无浏览器、无网络的纯逻辑测试
  integration/                # 接口实现与本地依赖的测试
  e2e/                        # 真实 Playwright + 受控页面
  fixtures/web-app/           # 查询、详情、表单、错误、弹窗测试页面
```

禁止建立万能的 `utils.py`、`agent.py` 或超过单一职责的 `service.py`。一个文件只处理一个边界内的职责；新增逻辑先判断属于领域、应用编排、浏览器适配、工具、验证还是基础设施。

### 2.2 关键依赖关系

| 模块 | 可以依赖 | 不可以依赖 |
| --- | --- | --- |
| `domain` | Python 标准库、Pydantic | HTTP、Playwright、数据库、模型 SDK |
| `application` | `domain`、ports | FastAPI 路由、具体 Playwright 类 |
| `browser` | `domain` ports、Playwright | API 路由、数据库实现 |
| `tools` / `verification` | `domain`、`browser` 暴露的接口 | FastAPI、仓储具体实现 |
| `infrastructure` | ports、第三方 SDK | `api` 路由 |
| `api` | `application`、请求 schema | Playwright 细节 |

## 3. 类、函数与接口设计

以下 Python 类型是实现时的接口契约。领域数据以 Pydantic `BaseModel` 和 `Enum` 表示；端口以 `typing.Protocol` 表示。不得将模型的自由文本直接传给 `BrowserWorker`；它必须先通过 `Action` 判别联合模型校验，再交由工具白名单执行。

```ts
from datetime import datetime
from enum import StrEnum
from typing import Any, Literal, Protocol
from pydantic import BaseModel, Field

# domain/task.py
class TaskStatus(StrEnum):
    QUEUED = "QUEUED"; RUNNING = "RUNNING"; SUCCEEDED = "SUCCEEDED"
    FAILED = "FAILED"; CANCELLED = "CANCELLED"; NEEDS_CONFIRMATION = "NEEDS_CONFIRMATION"

class TaskOptions(BaseModel):
    timeout_ms: int = 120_000
    max_retries: int = 2

class TaskInput(BaseModel):
    instruction: str = Field(min_length=1)
    start_url: str | None = None
    context: dict[str, str] = Field(default_factory=dict)
    options: TaskOptions = Field(default_factory=TaskOptions)

class Task(BaseModel):
    id: str; input: TaskInput; status: TaskStatus; created_at: datetime

# domain/action.py：实际实现使用 Pydantic 判别联合（此处省略各 Action 子类）
class ElementRef(BaseModel):
    element_id: str
    locator_hints: list["LocatorHint"]

class ToolResult(BaseModel):
    ok: bool; summary: str; data: Any | None = None; error: "AgentError | None" = None

class VerificationResult(BaseModel):
    passed: bool; reason: str; evidence: dict[str, Any]

# domain/ports/model_client.py
class ModelClient(Protocol):
    async def plan(self, *, task: TaskInput, observation: "Observation",
                   memory: "SharedMemory", history: list["StepRecord"]) -> "Action": ...

# domain/ports/browser_session.py
class BrowserSession(Protocol):
    async def create_tab(self, url: str | None = None) -> "TabId": ...
    async def close(self) -> None: ...

# application/agent_runner.py
class AgentRunner(Protocol):
    async def run(self, task_id: str, cancel_event: "asyncio.Event") -> "RunOutcome": ...
```

主要类和函数：

| 文件 | 导出 | 职责 |
| --- | --- | --- |
| `application/task_service.py` | `create(input)`, `get(task_id)`, `cancel(task_id)` | 写入任务、触发调度、返回任务视图；不执行浏览器操作。 |
| `application/run_scheduler.py` | `enqueue(task_id)`, `cancel(task_id)`, `drain()` | 管理 `asyncio.Semaphore`、任务队列与取消事件，确保结束后释放资源。 |
| `application/agent_runner.py` | `run(task_id, cancel_event)` | 串联观察、规划、调用、校验、记录、重试和终态。 |
| `browser/tab_manager.py` | `open()`, `activate()`, `current()`, `close()`, `snapshot()` | 每次执行独享；管理 Page、Popup、新标签及共享页面状态。 |
| `browser/observation_builder.py` | `build(page)` | 输出有限 token 的可见 DOM 语义树，生成 element_id 和定位提示。 |
| `browser/page_agent_bridge.py` | `build(page)`, `available()` | 定义可选页面语义能力的端口；不可用时由 `ObservationBuilder` 兜底。 |
| `infrastructure/page_agent/injected_bridge.py` | `build(page)` | 注入或调用经核验的 Page-Agent，并委托 mapper 转为领域 observation。 |
| `browser/browser_worker.py` | `navigate`, `click`, `fill`, `select`, `extract`, `scroll` | 封装 Playwright 异步原子操作与统一错误转换。 |
| `tools/tool_registry.py` | `execute(action, context)` | 仅分发已注册操作，验证参数和授权策略。 |
| `verification/action_verifier.py` | `verify(before, action, result, after)` | 聚合 URL、DOM、表单和反馈验证器，返回可解释证据。 |
| `application/retry_policy.py` | `next(error, attempt)` | 对可恢复错误给出等待、重观察或终止决策。 |

`composition_root.py` 负责把 `PlaywrightSession`、`OpenAIClient`、`SqliteRepository` 等具体实现注入 `AgentRunner`。除该文件外，任何业务模块不得实例化数据库、浏览器或模型客户端；应通过构造函数接收 `Protocol` 类型依赖。

## 4. 数据、状态与异常处理

### 4.1 数据结构

```ts
from datetime import datetime
from typing import Any, Literal
from pydantic import BaseModel, Field

# domain/observation.py
class Viewport(BaseModel):
    width: int; height: int; scroll_y: int

class ElementCandidate(BaseModel):
    element_id: str
    role: str | None = None
    name: str | None = None
    kind: Literal["link", "button", "input", "select", "textarea", "text", "other"]
    visible: bool
    enabled: bool | None = None
    locator_hints: list[LocatorHint]

class Observation(BaseModel):
    url: str; title: str; viewport: Viewport
    elements: list[ElementCandidate]  # 仅可见、可交互或任务相关的节点
    text_summary: str
    page_signals: PageSignals
    captured_at: datetime

# domain/run.py
class StepRecord(BaseModel):
    index: int; phase: RunPhase
    observation_artifact_id: str | None = None
    action: Action | None = None
    tool_result: ToolResult | None = None
    verification: VerificationResult | None = None
    duration_ms: int
    error: AgentError | None = None

class MemoryValue(BaseModel):
    value: Any; source_step: int
SharedMemory = dict[str, MemoryValue]
```

关系型存储至少有 `tasks`、`runs`、`steps` 和 `artifacts` 四类记录。大体积 DOM、截图、trace 只存入 ArtifactStore，数据库保存 `artifactId`、类型、校验和、路径/对象键和保留时间。机密字段、Cookie、密码、令牌和填写值必须按规则脱敏后才允许进入日志与工件。

### 4.2 状态流转

任务状态：

```text
QUEUED -> RUNNING -> SUCCEEDED
                  -> FAILED
                  -> CANCELLED
                  -> NEEDS_CONFIRMATION
```

一次 `RUNNING` 任务内部遵循：

```text
OBSERVE -> PLAN -> EXECUTE -> VERIFY
   ^                     |       |
   |---- retry/reobserve-+       +-> COMPLETE
                                 +-> FAIL
                                 +-> NEEDS_CONFIRMATION
```

规则：

- 每次 `EXECUTE` 前持久化 action 与操作前 observation；每次 `VERIFY` 后持久化结果与操作后 observation。
- `finish` action 仍需检查任务目标是否满足；模型声明完成不能直接使任务成功。
- 取消、总超时、浏览器崩溃和进程中止都通过 `asyncio.Event` 与 `asyncio.timeout()` 向下传播；最终在 `finally` 中关闭 context、写终态并释放调度槽位。
- 任务状态只允许按上图单向流转。终态更新应使用版本号或条件更新，避免取消与成功并发覆盖。

### 4.3 异常分类与恢复

| 错误码 | 示例 | 默认处理 |
| --- | --- | --- |
| `MODEL_INVALID_ACTION` | 输出不符合 schema、引用不存在元素 | 记录并重新观察/规划，最多 2 次。 |
| `LOCATOR_NOT_FOUND` | DOM 已变化、元素消失 | 重新观察，必要时重新规划。 |
| `ACTION_TIMEOUT` | 点击/导航超时 | 采集页面信号后重试一次；仍失败则终止。 |
| `VERIFICATION_FAILED` | URL、表单值、成功提示不符合预期 | 不盲目重复提交；重新规划或失败。 |
| `BROWSER_CRASHED` | context/page 意外关闭 | 关闭残余资源，若任务幂等才从最近安全步骤重启。 |
| `POLICY_BLOCKED` | 非授权域名、危险操作、敏感数据 | `NEEDS_CONFIRMATION` 或 `FAILED`，不重试。 |
| `TASK_CANCELLED` | 用户取消或服务关闭 | 写入 `CANCELLED`，停止所有后续工具调用。 |

重试必须有总次数、总时长和动作幂等性限制。`fill`、查询和提取一般可重试；提交表单、购买、删除、发信等有副作用的动作在校验不明时禁止自动重试。

## 5. 开发顺序、测试与验收

### 阶段 1：工程骨架与领域契约（P0）

实现 `domain` Pydantic 模型、API schema、环境配置、FastAPI 健康检查、任务创建/查询 API，以及内存仓储的开发实现；同时创建 `.env.example`、`.gitignore` 和 PowerShell 启动/测试脚本。

测试：对 schema、状态转移、错误映射、仓储条件更新写 pytest 单测；执行 Ruff、mypy（strict）和 Pyright 检查。

验收：在 Windows 本机执行 `scripts/run.ps1` 后，服务仅监听 `127.0.0.1:8000`；非法请求返回明确 4xx；创建任务后可查询 `QUEUED` 状态；领域层不依赖 Playwright/FastAPI；`var/` 与 `.env` 未被 Git 跟踪。

### 阶段 2：单页浏览器闭环（P0）

实现 Playwright Context 生命周期、`BrowserWorker`、`ObservationBuilder`、Locator 解析、五种基础工具和 `AgentRunner` 最小状态机；使用 `FakeModelClient` 固定输出动作序列。

测试：受控页面覆盖导航、点击、填写、选择、提取和滚动；单测覆盖可见性过滤和 Locator 优先级。

验收：一个任务能在独立 Context 中完成“访问查询页 → 查询 → 提取结果”，并保存每步 observation、action、result 和耗时；任一步异常后 Context 被关闭。

### 阶段 3：校验、重试与终止（P0）

实现四类验证器、错误分类、`RetryPolicy`、任务取消和总超时；接入真实 `ModelClient` 但保留 fake 实现作为回归测试依赖。

测试：制造 Locator 失效、导航超时、页面报错和错误模型输出；验证取消期间无后续操作。

验收：成功不能只依赖模型文本；错误动作会得到结构化失败原因；可恢复错误最多按配置次数重试；超时和取消后状态、资源和日志一致。

### 阶段 4：多 Tab 与跨页记忆（P1）

实现 `TabManager`、Popup 处理、活动页切换和 `SharedMemory`；由规划动作显式读写记忆，避免页面状态散落在全局变量。

测试：受控站点覆盖“搜索页打开详情页 → 提取字段 → 切换至后台表单填写”。

验收：每个 tab 有唯一 ID 和生命周期记录；跨页字段可追溯到来源步骤；关闭任务后没有遗留 Page 或 Context。

### 阶段 4.5：Page-Agent 适配验证（P1）

在不改变 `AgentRunner`、`Action` 或 `ToolRegistry` 的前提下，实现 `PageAgentBridge` 的最小适配器；并保留 `ObservationBuilder` 作为默认回退。接入前记录所用 Page-Agent 的准确版本、许可证、来源、安装方式及其公开 API 证据。

测试：同一组受控页面分别以纯 Playwright observation 和 Page-Agent observation 执行；验证 mapper 不泄漏专有对象，故意让 Page-Agent 初始化失败以验证回退路径。

验收：Page-Agent 不可用时任务仍可完成；可用时只改变 observation 生成，不改变动作白名单和校验逻辑；两种模式都能产出相同格式的步骤记录，并可比较元素命中率、任务成功率、单步耗时与 observation token 大小。

### 阶段 5：持久化、回放与评测（P1）

实现数据库仓储、工件存储、trace/截图可选采集、执行时间线查询和批量评测任务。

测试：数据库迁移测试、工件校验和测试、从历史 run 读取步骤并渲染时间线的集成测试。

验收：可按 taskId 取得完整步骤和工件引用；失败任务能定位到失败前后 observation、模型输出、工具结果和验证证据；敏感数据不出现在响应和日志中。

### 阶段 6：并发、治理与体验（P2）

实现调度队列、并发上限、指标、脱敏策略、危险操作确认、人机接管和性能优化。

测试：并发任务隔离、队列压力、浏览器崩溃、工件存储不可用、脱敏回归测试。

验收：达到配置的并发数仍无 Cookie/页面/内存共享；任务超额排队且可取消；指标可观察排队时间、成功率、重试率和浏览器资源；危险操作不经确认不会执行。

## 6. 已确定与待确认的决策

已确定：首期只部署在本地 Windows 环境，运行单个 Uvicorn 进程和本机 Chromium，不使用 Docker、Kubernetes、远程 CDP 或浏览器池。

仍需确认：

1. 允许访问的域名、登录方式及是否提供测试账号；没有这些条件不得对真实站点执行写操作。
2. LLM 供应商、模型、预算、输出格式和数据出境限制。
3. 哪些操作属于危险动作，以及确认请求由 API、前端还是人工坐席处理。
4. 本机可接受的并发量、单任务最大时长、日志/快照保留周期与磁盘预算。
5. 第一批验收业务流程及明确的成功断言；建议先采用完全受控的本地测试站点。
6. Page-Agent 的准确仓库地址、版本、许可证、依赖方式、可公开调用 API 及是否允许源码二开；确认后才能决定 adapter 仅调用、封装还是 fork 维护。
